# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.30.0

# Database
sqlalchemy[asyncio]>=2.0.30
aiosqlite>=0.20.0
alembic>=1.13.1

# Validation & settings
pydantic>=2.7.0
pydantic-settings>=2.3.0

# Cache & task queue
redis[asyncio]>=5.0.6
celery[redis]>=5.4.0
kombu>=5.3.4

# File handling
python-multipart>=0.0.9
aiofiles>=23.2.1
python-magic>=0.4.27

# Logging
structlog>=24.2.0
rich>=13.7.0

# HTTP client (for health checks)
httpx>=0.27.0

# Media processing
ffmpeg-python>=0.2.0

# Speech recognition
# whisper.cpp is a compiled C++ binary — no Python package required here.
# Install instructions: docs/testing/WHISPER_CPP_SETUP.md

# Source separation (Phase 5 — optional, Celery worker only)
# Do NOT install unpinned torch — TorchAudio >=2.9 breaks Demucs WAV export.
# See backend/requirements-ml.txt and docs/reports/PHASE5_M2_DEMUCS_DEPENDENCY_ANALYSIS.md
#   pip install -r requirements-ml.txt

# Utilities
python-dotenv>=1.0.1
